let canvasX = 100;
let canvasY = 100;

function setup(){
  createCanvas(canvasX, canvasY, SVG);
  noFill();
}

function draw(){
  for(let i = 0; i < canvasX; i += 25){
    for(let j = 0; j < canvasY; j += 25){
    }
  }

  noLoop();
}